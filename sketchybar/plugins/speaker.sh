#!/bin/bash

# OUTPUT_NAME=$(SwitchAudioSource -t output -c)
# VOLUME=$(osascript -e 'output volume of (get volume settings)')

# case $OUTPUT_NAME in
# 'MacBook Pro Microphone')
#     OUTPUT_NAME="Mic"
#     ;;
# 'USB PnP Audio Device')
#     OUTPUT_NAME="USB"
#     ;;
# esac

# ICON=󰕾
# HIGHLIGH=off
# if [ $VOLUME -eq 0 ]; then
#     ICON=󰕾
#     HIGHLIGH=on
# fi

# sketchybar --set $NAME label="$OUTPUT_NAME ($VOLUME%)" icon=$ICON icon.highlight=$HIGHLIGH